# PORT-CLI-001 — CLI/HTTP Client Port Plan

**Task:** PORT-CLI-001  
**Mode:** Plan  
**Status:** Ready for Code execution  
**Date:** 2026-08-10  
**Model:** Qwen3.5-122B

---

## 1. Executive Summary

This plan identifies the VS Code CLI/HTTP communication layer components and maps them to the existing Visual Studio implementation. The VS Code implementation is the source of truth.

**Key Finding:** The Visual Studio extension has a solid foundation for the CLI/HTTP layer but is missing several features present in VS Code, particularly around session visibility tracking and directory management.

---

## 2. VS Code Source Components

### 2.1 ServerManager (`server-manager.ts`)

| Aspect | VS Code Implementation |
|--------|----------------------|
| **File** | `packages/kilo-vscode/src/services/cli-backend/server-manager.ts` |
| **Spawn** | `spawn(cliPath, ["serve", "--port", "0"])` |
| **Port discovery** | Regex parse stdout for `listening on http://127.0.0.1:PORT` |
| **Timeout** | 30 seconds (`STARTUP_TIMEOUT_SECONDS`) |
| **Password** | `crypto.randomBytes(32).toString("hex")` via `KILO_SERVER_PASSWORD` env |
| **Env vars** | 20+ vars: KILO_CLIENT, KILO_PLATFORM, KILO_TELEMETRY_LEVEL, KILO_APP_NAME, KILO_MACHINE_ID, KILO_VSCODE_VERSION, NODE_USE_SYSTEM_CA, MIMALLOC_PURGE_DELAY, KILO_PARENT_PID, etc. |
| **Process cleanup** | SIGTERM then SIGKILL fallback after 5s |

### 2.2 KiloConnectionService (`connection-service.ts`)

| Aspect | VS Code Implementation |
|--------|----------------------|
| **File** | `packages/kilo-vscode/src/services/cli-backend/connection-service.ts` (936 lines) |
| **Pattern** | Singleton, lazy startup |
| **Client** | `createKiloClient({ baseUrl, headers: { Authorization: Basic auth } })` |
| **SSE** | `new SdkSSEAdapter(client)` |
| **Health poll** | 10s interval (`HEALTH_POLL_INTERVAL_MS = 10000`) |
| **Session visibility** | `registerVisible(key, ids)`, `registerAttached(key, ids)`, `flushViewed()` |
| **Directory tracking** | `trackDirectory(dir)`, `getKnownDirectories()` |
| **Permission tracking** | `recordPermissionDirectory()`, `getPermissionDirectory()`, `clearPermissionDirectory()` |
| **Question tracking** | `recordQuestionDirectory()`, `getQuestionDirectory()`, `clearQuestionDirectory()` |
| **Message session mapping** | `recordMessageSessionId()`, `pruneSession()` |
| **State machine** | connecting → connected / disconnected / error |
| **Checkin timer** | 60s interval for `flushViewed()` |

### 2.3 SdkSSEAdapter (`sdk-sse-adapter.ts`)

| Aspect | VS Code Implementation |
|--------|----------------------|
| **File** | `packages/kilo-vscode/src/services/cli-backend/sdk-sse-adapter.ts` (292 lines) |
| **Endpoint** | `client.global.event()` (SDK AsyncGenerator) |
| **Reconnection** | `while (!signal.aborted)` loop, 250ms delay |
| **Heartbeat** | 15s timeout (`HEARTBEAT_TIMEOUT_MS`) |
| **Max delay** | 5s (`MAX_RECONNECT_DELAY_MS`) |
| **Exponential backoff** | Yes, doubles delay on failure |
| **Event normalization** | Transforms sync events to flat format |
| **Handlers** | `onEvent`, `onError`, `onStateChange` |

### 2.4 HTTP Client

| Aspect | VS Code Implementation |
|--------|----------------------|
| **Source** | `@kilocode/sdk/v2/client` - `createKiloClient()` |
| **Auth** | Basic auth via `Authorization: Basic kilo:password` |
| **Base URL** | `http://127.0.0.1:PORT` |

---

## 3. Visual Studio Implementation

### 3.1 CliBackendManager (`CliBackendManager.cs`)

| Aspect | Visual Studio | Status | Gap |
|--------|---------------|--------|-----|
| **File** | `KiloVisualStudioExtension/CliBackendManager.cs` (286 lines) | EXISTS | |
| **Spawn** | `Process.Start(cliPath, "serve --port 0")` | ADAPTED | |
| **Port discovery** | Regex parse stdout | EXACT | |
| **Timeout** | 30s | EXACT | |
| **Password** | NOT GENERATED | **MISSING** | Must generate random password |
| **Env vars** | Only KILO_CLIENT, KILO_PLATFORM, KILO_APP_NAME | **PARTIAL** | Missing 17+ env vars |
| **Process cleanup** | Kill + WaitForExit(5s) | ADAPTED | |

### 3.2 KiloConnectionService (`KiloConnectionService.cs`)

| Aspect | Visual Studio | Status | Gap |
|--------|---------------|--------|-----|
| **File** | `KiloVisualStudioExtension/KiloConnectionService.cs` (518 lines) | EXISTS | |
| **Pattern** | Singleton, lazy startup | EXACT | |
| **Client** | `HttpClientWrapper` (manual) | ADAPTED | Not using SDK |
| **SSE** | `SseClient` | ADAPTED | |
| **Health poll** | 10s interval | EXACT | |
| **Session visibility** | NOT IMPLEMENTED | **MISSING** | Must add registerVisible, registerAttached, flushViewed |
| **Directory tracking** | NOT IMPLEMENTED | **MISSING** | Must add trackDirectory, getKnownDirectories |
| **Permission tracking** | NOT IMPLEMENTED | **MISSING** | Must add recordPermissionDirectory etc. |
| **Question tracking** | NOT IMPLEMENTED | **MISSING** | Must add recordQuestionDirectory etc. |
| **Message session mapping** | NOT IMPLEMENTED | **MISSING** | Must add recordMessageSessionId, pruneSession |
| **Checkin timer** | NOT IMPLEMENTED | **MISSING** | Must add 60s flushViewed timer |

### 3.3 SseClient (`SseClient.cs`)

| Aspect | Visual Studio | Status | Gap |
|--------|---------------|--------|-----|
| **File** | `KiloVisualStudioExtension/SseClient.cs` (356 lines) | EXISTS | |
| **Endpoint** | `GET /global/event` | EXACT | |
| **Reconnection** | 250ms delay | EXACT | |
| **Heartbeat** | 15s timeout | EXACT | |
| **Max delay** | NOT IMPLEMENTED | **MISSING** | No MAX_RECONNECT_DELAY_MS |
| **Exponential backoff** | NOT IMPLEMENTED | **MISSING** | No exponential backoff |
| **Event normalization** | NOT IMPLEMENTED | **MISSING** | No sync event transformation |

### 3.4 HttpClientWrapper (`HttpClientWrapper.cs`)

| Aspect | Visual Studio | Status | Gap |
|--------|---------------|--------|-----|
| **File** | `KiloVisualStudioExtension/HttpClientWrapper.cs` (198 lines) | EXISTS | |
| **Auth** | Basic auth | EXACT | |
| **Methods** | GET, POST with JSON | ADAPTED | |

---

## 4. Test Mapping

### 4.1 VS Code Tests (Individual Enumeration)

| # | Test File | Describe/Context | Test Name | Behavior Tested | VS Equivalent | Status |
|---|-----------|------------------|-----------|-----------------|---------------|--------|
| 1 | `connection-service.test.ts` | `KiloConnectionService sandbox preference` | `uses workspace state instead of extension-global state` | Service uses workspaceState not globalState | None | MISSING |
| 2 | `connection-service.test.ts` | `KiloConnectionService clients` | `returns a connected client without a workspace folder` | getClientAsync returns connected client | None | MISSING |
| 3 | `connection-service.test.ts` | `KiloConnectionService viewed sessions` | `keeps Agent Manager sessions when sidebar visibility changes during a flush` | AM sessions retained during sidebar flush | None | MISSING |
| 4 | `connection-service.test.ts` | `KiloConnectionService viewed sessions` | `window focus gates viewer.active but not attachment` | Window focus affects viewer.active only | None | MISSING |
| 5 | `connection-service.test.ts` | `KiloConnectionService viewed sessions` | `sends snapshots while remote control is disabled` | Viewed snapshots sent even when remote disabled | None | MISSING |
| 6 | `connection-service.test.ts` | `KiloConnectionService drainPendingPrompts` | `ignores stale NotFoundError replies while draining permissions` | NotFoundError ignored during drain | None | MISSING |
| 7 | `connection-service.test.ts` | `KiloConnectionService drainPendingPrompts` | `drains four directories concurrently and suggestions once` | Concurrent drain with correct ordering | None | MISSING |
| 8 | `connection-service.test.ts` | `KiloConnectionService drainPendingPrompts` | `waits for active drains and skips queued directories after a failure` | Active drain waits, failures skip queued | None | MISSING |

### 4.2 Visual Studio Tests (No Verified VS Code Source)

| Test File | Test Class | Status | Notes |
|-----------|------------|--------|-------|
| `KiloProviderSessionRefreshTests.cs` | `KiloProviderSessionRefreshTests` | MISSING | No verified VS Code source test |
| `SessionStreamSchedulerTests.cs` | `SessionStreamSchedulerTests` | MISSING | No verified VS Code source test |
| `SSEEventFilteringTests.cs` | `SSEEventFilteringTests` | MISSING | No verified VS Code source test |

### 4.3 Test Status Summary

| Status | Count |
|--------|-------|
| PORTED_1_TO_1 | 0 |
| PORTED_ADAPTED | 0 |
| MISSING | 8 VS Code tests have no Visual Studio equivalent |

**Note:** Per SPEC.md Section 8, Visual Studio tests without verified VS Code counterparts must be classified as MISSING, not PORTED_1_TO_1. The existing Visual Studio tests may have been independently created.

---

## 5. Required Changes

### 5.1 High Priority (Required for Parity)

| # | Component | Change Type | Description |
|---|-----------|-------------|-------------|
| 0 | Generated C# HTTP Client | ADD | Generate C# SDK client from OpenAPI source (see Section 13 for generation chain) |
| 1 | CliBackendManager | MODIFY | Generate random password, add KILO_SERVER_PASSWORD env var |
| 2 | CliBackendManager | MODIFY | Add CLI-functional env vars (see Section 12 for classification) |
| 3 | KiloConnectionService | MODIFY | Use generated C# SDK client instead of HttpClientWrapper |
| 4 | KiloConnectionService | ADD | Session visibility tracking (registerVisible, registerAttached, flushViewed) |
| 5 | KiloConnectionService | ADD | Directory tracking (trackDirectory, getKnownDirectories) |
| 6 | KiloConnectionService | ADD | Permission/question directory tracking |
| 7 | KiloConnectionService | ADD | Message session ID mapping |
| 8 | KiloConnectionService | ADD | Checkin timer (60s flushViewed) |
| 9 | SseClient | MODIFY | Add exponential backoff with MAX_RECONNECT_DELAY_MS |

### 5.3 Out of Scope for PORT-CLI-001

| # | Component | Change Type | Description |
|---|-----------|-------------|-------------|
| 10 | SSE Event Normalization | DEFER | Sync event transformation can be deferred to a later task |
| 11 | drainPendingPrompts | DEFER | Implementation can be deferred to a later task |

---

## 6. Implementation Order

### Phase 1: Generated C# SDK Client

0. **Generate C# SDK client from OpenAPI** - Use @hey-api/openapi-ts or OpenAPI Generator to produce C# client from packages/sdk/openapi.json

### Phase 2: Core Infrastructure

1. **Update CliBackendManager** - Generate password, add env vars
2. **Integrate generated C# SDK client** - Replace HttpClientWrapper with generated client in KiloConnectionService
3. **Add session visibility to KiloConnectionService** - registerVisible, registerAttached, flushViewed
3. **Add directory tracking** - trackDirectory, getKnownDirectories

### Phase 3: Event Handling

4. **Add directory tracking** - trackDirectory, getKnownDirectories
5. **Add permission/question tracking** - recordPermissionDirectory, recordQuestionDirectory
6. **Add message session mapping** - recordMessageSessionId, pruneSession
7. **Add checkin timer** - 60s flushViewed

### Phase 4: SSE Enhancement

8. **Add exponential backoff to SseClient** - MAX_RECONNECT_DELAY_MS

### Phase 5: Testing

9. **Port connection service tests** - Create equivalents for VS Code tests

---

## 7. Files to Modify

| File | Action | Reason |
|------|--------|--------|
| `CliBackendManager.cs` | MODIFY | Add password generation, env vars |
| `KiloConnectionService.cs` | MODIFY | Integrate generated C# SDK client, add session visibility, directory tracking |
| `SseClient.cs` | MODIFY | Add exponential backoff |
| `Generated/` (new) | CREATE | Generated C# SDK client from OpenAPI (do not modify manually) |

---

## 8. Validation Steps

1. Build succeeds without errors
2. All existing tests pass
3. New functionality has equivalent test coverage
4. No regression in existing behavior

---

## 9. Blockers

| Blocker | Resolution |
|---------|------------|
| None identified yet | Proceed to Code phase |

---

## 10. Source Tracking

**VS Code source revision:** `1083bb82b65e986dfbc7092647b6ee2650951265`  
**VS Code files inspected:**
- `packages/kilo-vscode/src/services/cli-backend/connection-service.ts`
- `packages/kilo-vscode/src/services/cli-backend/sdk-sse-adapter.ts`
- `packages/kilo-vscode/src/services/cli-backend/server-manager.ts`
- `packages/kilo-vscode/src/services/cli-backend/connection-service.test.ts`

---

## 11. Environment Variable Classification

Based on inspection of `server-manager.ts`, environment variables are classified as follows:

### CLI-Functional (Must Preserve)

| Variable | Purpose | Visual Studio Action |
|----------|---------|---------------------|
| `KILO_SERVER_PASSWORD` | CLI authentication | **REQUIRED** - Generate random password |
| `KILO_PARENT_PID` | Parent watchdog (CLI exits if extension host dies) | **REQUIRED** - Set to extension process PID |
| `MIMALLOC_PURGE_DELAY` | Memory allocator behavior (prevents RSS growth) | **REQUIRED** - Set to "0" |
| `NODE_USE_SYSTEM_CA` | TLS trust store | **REQUIRED** - Set to "1" |
| `NODE_EXTRA_CA_CERTS` | Custom CA bundle (if configured) | **REQUIRED** - Read from VS Code config |
| `NODE_TLS_REJECT_UNAUTHORIZED` | TLS verification opt-out | **REQUIRED** - Honor VS Code proxyStrictSSL |
| `HTTP_PROXY` / `HTTPS_PROXY` / `NO_PROXY` | Corporate proxy support | **REQUIRED** - Map from VS Code http.* settings |

### VS Code-Specific (Not Required for Visual Studio)

| Variable | Purpose | Visual Studio Action |
|----------|---------|---------------------|
| `KILO_VSCODE_VERSION` | VS Code version metadata | **NOT REQUIRED** - VS Code specific |
| `KILO_EDITOR_NAME` | Editor name (includes VS Code version) | **NOT REQUIRED** - VS Code specific |
| `KILO_VSCODE_VERSION` | Duplicate VS Code version | **NOT REQUIRED** - VS Code specific |
| `KILO_TELEMETRY_LEVEL` | Telemetry based on VS Code settings | **VERIFY** - Check VS Code telemetry behavior and Visual Studio telemetry API |
| `KILO_MACHINE_ID` | VS Code machine ID | **VERIFY** - Check if Visual Studio provides equivalent machine identifier |
| `KILO_APP_NAME` | Application name | **OPTIONAL** - Can set to "kilo-code" |
| `KILO_APP_VERSION` | Extension version | **OPTIONAL** - Can set from VSIX manifest |
| `KILOCODE_VERSION` | Extension version | **OPTIONAL** - Can set from VSIX manifest |
| `KILOCODE_EDITOR_NAME` | Editor name with version | **NOT REQUIRED** - VS Code specific |
| `KILO_ENABLE_QUESTION_TOOL` | VS Code question tool flag | **NOT REQUIRED** - VS Code specific |
| `KILOCODE_FEATURE` | Feature flag for VS Code | **NOT REQUIRED** - VS Code specific |
| `KILO_DISABLE_CLAUDE_CODE` | Claude compatibility flag | **OPTIONAL** - If relevant to VS |
| `KILO_DISABLE_CODEBASE_INDEXING` | Indexing control | **ADAPT** - If indexing is supported |
| `KILO_DISABLE_CHANNEL_DB` | Channel DB control | **OPTIONAL** - If relevant |

### Visual Studio Equivalents Needed

| VS Code Variable | Visual Studio Equivalent |
|------------------|-------------------------|
| `KILO_CLIENT=vscode` | `KILO_CLIENT=visualstudio` |
| `KILO_PLATFORM=vscode` | `KILO_PLATFORM=visualstudio` |
| `VS Code http.proxy` | `HTTP_PROXY` / `HTTPS_PROXY` |
| `VS Code http.noProxy` | `NO_PROXY` |
| `VS Code http.proxyStrictSSL` | `NODE_TLS_REJECT_UNAUTHORIZED` |

---

## 12. Plan Status

**Status:** Ready for Code execution

This plan provides enough detail for a Code-mode agent to implement the required changes without additional architectural decisions.

The plan distinguishes between:
- **EXACT**: Identical behavior
- **ADAPTED**: Platform-specific implementation with equivalent behavior
- **PARTIAL**: Some features implemented, gaps identified
- **MISSING**: Not implemented in Visual Studio

---

## 13. Generated C# SDK Client - Generation Chain

**Question answered:** "Quelle est la chaîne exacte permettant de partir de la source OpenAPI de Kilo et de produire le client C# utilisé par l'extension Visual Studio ?"

### 13.1 Source OpenAPI

| Item | Value |
|------|-------|
| **Source file** | `packages/sdk/openapi.json` (in this repository) |
| **OpenAPI version** | 3.1.0 |
| **Generated by** | `bun dev generate` command in `packages/opencode` |
| **Source SHA (at VS Code revision)** | `1083bb82b65e986dfbc7092647b6ee2650951265` |
| **openapi.json SHA** | To be recorded at generation time |

### 13.2 TypeScript SDK Generation (Reference)

| Item | Value |
|------|-------|
| **Generator** | `@hey-api/openapi-ts` version `0.90.10` |
| **Configuration** | `packages/sdk/js/script/build.ts` |
| **Input** | `./openapi.json` |
| **Output** | `./src/v2/gen` |
| **Plugins** | `@hey-api/typescript`, `@hey-api/sdk` (instance: `KiloClient`), `@hey-api/client-fetch` |
| **Command** | `bun run build` in `packages/sdk/js` |

### 13.3 C# SDK Generation (To Be Implemented)

| Item | Value |
|------|-------|
| **Input** | `packages/sdk/openapi.json` (same source as TypeScript SDK) |
| **Generator options** | Must be verified for .NET Framework 4.8.1 compatibility |
| **Recommended generators** | `openapi-generator-cli` (C# client), `Kiota` (Microsoft), or `@hey-api/openapi-ts` if C# output is available |
| **Output directory** | `KiloVisualStudioExtension/Generated/` (do not modify manually) |
| **Constraints** | Must target .NET Framework 4.8.1 (`net481`) |

### 13.4 Generation Requirements

1. **Deterministic and reproducible** - Same OpenAPI source + same generator version = same output
2. **No fork dependencies** - All references must use upstream Kilo repository and public packages
3. **Separation of concerns** - Generated code must be in a separate directory from handwritten code
4. **Version tracking** - Record OpenAPI SHA, generator version, and configuration used

### 13.5 BLOCKER: Generator Selection

**Status:** Requires verification before implementation

The plan must verify which OpenAPI-to-C# generator is compatible with:
- .NET Framework 4.8.1 target
- The specific OpenAPI 3.1.0 specification used by Kilo
- Required features: SSE/Server-Sent Events support, Basic Auth, JSON serialization

**Recommended approach:**
1. Test `openapi-generator-cli` with `csharp-netcore` generator
2. Test Microsoft Kiota for C# generation
3. Verify SSE support in generated client (required for `/global/event` endpoint)
4. Verify Basic Auth support
5. Confirm .NET Framework 4.8.1 compatibility

**If no suitable generator is found:** Document the blocker and propose an alternative (e.g., manual client implementation with OpenAPI-based validation).

### 13.6 Registration Requirements

When generating the C# client, record:
- OpenAPI source SHA
- Generator name and version
- Generator configuration file
- Generation date
- Any patches or workarounds applied

---